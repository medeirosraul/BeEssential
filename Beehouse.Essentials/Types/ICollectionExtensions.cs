using Beehouse.Essentials.Entities;
using System.Collections.Generic;
using System.Linq;

namespace Beehouse.Essentials.Types
{
    public static class ICollectionExtensions
    {
    //    public static ICollection<TConvertible> FromEntity<TConvertible, TEntity>(this ICollection<TConvertible> collection)
    //        where TConvertible : EntityConvertible<TEntity>, new()
    //        where TEntity : BaseEntity
    //    {
    //        collection.Select(s => new TConvertible().FromEntity(s))
    //    }
    }
}
